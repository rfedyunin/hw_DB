# Домашнее задание к занятию "`Базы данных, их типы`" - `Федюнин Р.Ю.`

---

### Задание 1. СУБД

`Крупная строительная компания, которая также занимается проектированием и девелопментом, решила создать правильную архитектуру для работы с данными. Ниже представлены задачи, которые необходимо решить для каждой предметной области.`

`Какие типы СУБД, на ваш взгляд, лучше всего подойдут для решения этих задач и почему?`

* `1.1. Бюджетирование проектов с дальнейшим формированием финансовых аналитических отчётов и прогнозирования рисков. СУБД должна гарантировать целостность и чёткую структуру данных.`

   * `Реляционная СУБД. Потому что нужны строгие схемы, целостность данных`

* `1.2. Под каждый девелоперский проект создаётся отдельный лендинг, и все данные по лидам стекаются в CRM к маркетологам и менеджерам по продажам. Какой тип СУБД лучше использовать для лендингов и для CRM? СУБД должны быть гибкими и быстрыми.`

   * `Для лендингов: Документоориентированная (MongoDB). Данные от пользователя могут приходить не заполненые поля, важно быстро записать без жёсткой структуры. Для CRM: Та же MongoDB или реляционная (PostgreSQL). По условию («гибкие и быстрые») лучше MongoDB.`

* `1.3. Отдел контроля качества решил создать базу по корпоративным нормам и правилам, обучающему материалу и так далее, сформированную согласно структуре компании. СУБД должна иметь простую и понятную структуру.`

   * `Документо-ориентированные СУБД. Данные чётко делятся по структуре компании (отдел → должность → документ). Удобно хранить дерево папок и правил без сложных связей`

* `1.4. Департамент логистики нуждается в решении задач по быстрому формированию маршрутов доставки материалов по объектам и распределению курьеров по маршрутам с доставкой документов. СУБД должна уметь быстро работать со связями.`

   * `Графовая СУБД (например, Neo4j). Потому что задача — быстро работать со связями. Реляционные БД считают такие маршруты долго`



---

### Задание 2. Транзакции

* `2.1. Пользователь пополняет баланс счёта телефона, распишите пошагово, какие действия должны произойти для того, чтобы транзакция завершилась успешно. Ориентируйтесь на шесть действий.`

   * `Начало транзакции (BEGIN) — фиксируем начало операции.`

   * `Блокировка счёта отправителя и списание суммы.`

   * `Блокировка счёта получателя и зачисление суммы.`

   * `Запись в журнал операций (лог) факта перевода.`

   * `Проверка целостности (у отправителя минус, у получателя плюс).`

   * `Фиксация транзакции (COMMIT) — если всё ОК, либо Откат (ROLLBACK) — если ошибка.`

---

### Задание 3. SQL vs NoSQL

* `3.1. Напишите пять преимуществ SQL-систем по отношению к NoSQL.`

   * `Строгая схема и целостность данных через ограничения (PRIMARY KEY, FOREIGN KEY, CHECK).`

   * `Поддержка сложных JOIN-запросов для связывания данных из разных таблиц.`

   * `Гарантия, что операция либо выполнится полностью, либо откатится (надёжность для финансов и критичных данных).`

   * `Стандартизированный язык запросов (SQL)`

   * `Больше инструментов мониторинга для решения проблем.`


### Задание 4. Кластеры

`Необходимо производить большое количество вычислений при работе с огромным количеством данных, под эту задачу выделено 1000 машин.`
`На основе какого критерия будете выбирать тип СУБД и какая модель распределённых вычислений здесь справится лучше всего и почему?`

   * `Критерий выбора: Горизонтальная масштабируемость и отказоустойчивость (данные должны реплицироваться и перераспределяться при падении узлов)`
   * `Модель распределённых вычислений: MapReduce или его современный аналог (например, распределённые SQL-движки типа Greenplum или ClickHouse).`
   * `Почему: На 1000 машинах данные не поместятся на одном диске, а обычные реляционные БД не умеют так сильно шардироваться. Модель "разделяй и властвуй" (разбить задачу на части → обработать на всех узлах → склеить результат) позволяет параллельно использовать все 1000 машин и не бояться, что одна из них упадёт.`
